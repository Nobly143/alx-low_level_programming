#include "main.h"
/**
 * _abs - checks the absolute value of any numer
 * @n: the number to compute
 * Return: the absolute value of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
