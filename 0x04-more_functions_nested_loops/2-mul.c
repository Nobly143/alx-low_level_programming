#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 * @a and @b: the integers to be multiplied
 * Return: the value of multiplying
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
	_putchar('\n');
}
